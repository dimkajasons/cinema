{
  "name": "cinema",
  "version": "1.0.0",
  "description": "1. run `npm install -g http-static` command\r 2. make sure you are in cinema folder\r 3. run `http-static` command in terminal",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {
    "axios": "^0.18.0",
    "jquery": "^3.3.1",
    "lodash": "^4.17.10"
  }
}
